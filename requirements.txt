# Core dependencies for PubMed RAG v2
biopython>=1.81
python-dotenv>=1.0.0
sentence-transformers>=2.2.2
scikit-learn>=1.3.0

# Additional dependencies (installed automatically with above packages)
# - torch (required by sentence-transformers)
# - transformers (required by sentence-transformers)
# - numpy (required by scikit-learn and sentence-transformers)

